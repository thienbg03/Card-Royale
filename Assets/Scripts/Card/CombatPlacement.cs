using UnityEngine;

public class CombatPlacement : MonoBehaviour
{
    public int ID;
    public Transform SpawnPosition;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
